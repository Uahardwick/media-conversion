<Window x:Class="MediaSuite.Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Media Tools Suite" Height="480" Width="780"
        MinHeight="360" MinWidth="640">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Media Tools Suite" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <DataGrid Grid.Row="1" x:Name="ComponentsGrid" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeight="36">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Component" Binding="{Binding DisplayName}" Width="180"/>
                <DataGridTextColumn Header="Installed" Binding="{Binding InstalledVersion}" Width="110"/>
                <DataGridTextColumn Header="Latest" Binding="{Binding LatestVersion}" Width="110"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                <DataGridTemplateColumn Header="" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{Binding ActionLabel}" Click="ActionButton_Click"
                                    IsEnabled="{Binding IsNotBusy}" Padding="6,2" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remove" Click="RemoveButton_Click"
                                    Visibility="{Binding RemoveVisibility}"
                                    IsEnabled="{Binding IsNotBusy}" Padding="6,2" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="CheckForUpdatesButton" Content="Check for Updates"
                    Click="CheckForUpdatesButton_Click" Padding="12,6"/>
            <TextBlock Grid.Column="1" x:Name="LastCheckedText" Text="Not checked yet"
                       VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="Gray"/>
        </Grid>
    </Grid>
</Window>
